/target

# Runtime state produced by the kernel (not source, not generated artifacts).
/evidence/*.jsonl
/checkpoints/*.json
/tasks/*.json
.intake/

# Refinery proposals and alternate-target build output are demonstrations,
# regenerable on demand — not committed.
/refinery/proposals/
/dist/
