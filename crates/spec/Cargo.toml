[package]
name = "spec"
description = "The pattern-language metamodel and compiler front-end: parse a harness.patterns.yaml, parse the composition algebra, and statically reject incomplete or unsafe pattern compositions."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "spec"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_yaml = { workspace = true }
